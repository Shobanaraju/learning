import { FetchMarksResponse } from './fetch-marks-response';

describe('FetchMarksResponse', () => {
  it('should create an instance', () => {
    expect(new FetchMarksResponse()).toBeTruthy();
  });
});
